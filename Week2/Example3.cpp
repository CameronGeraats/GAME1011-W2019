#include <iostream>
#include "Rectangle.h"
using namespace std;

/*
int main()
{
	Rectangle box;
	double boxLength, boxWidth;
	
	// Ask the user for length and width-- User Experience with a good UI
	cout << "What is the length. Positive values only" << endl;
	cin >> boxLength;
	cout << "What is the width. Positive values only" << endl;
	cin >> boxWidth;

	if (!box.setLength(boxLength))
	{
		cout << "Invalid box length entered" << endl;
	}
	else if (!box.setWidth(boxWidth))
	{
		cout << "Invalid box width entered" << endl;
	}
	else
	{
		cout << "Length: " << box.getLength() << endl;
		cout << "Width: " << box.getWidth() << endl;
		cout << "Area: " << box.getArea() << endl;
	}

	return 0;
}
*/